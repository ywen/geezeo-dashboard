#account-balance
  All Accounts Balance:
  =@presenter.formatted_balance
.line
#accounts-list
  Accounts:
  %ul
    - @presenter.each_account do |account|
      %li=link_to "#{account.name} ( #{account.formatted_balance} )", account_transactions_path(account_id: account.id)
